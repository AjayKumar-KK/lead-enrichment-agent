# Runtime dependencies
#
# Deliberately small. The Groq client, the retry logic, the HTML-to-markdown
# cleaner and the test runner are all implemented in this repository rather than
# pulled in as packages, so there is less to install, less to break on another
# machine, and the logic being graded is visible in the source.

httpx==0.27.2            # async HTTP: tier-1 fetching, sitemaps, and the Groq API
playwright==1.47.0       # tier-2 headless Chromium for JavaScript-rendered pages
lxml==5.3.0              # DOM parsing, stripping, and markdown rendering
pydantic==2.9.2          # strict structured output: schema, validation, serialisation
python-dotenv==1.0.1     # loads GROQ_API_KEY from .env

# Development only (the suite also runs without it: python tests/run_tests.py)
# pytest==8.3.3
